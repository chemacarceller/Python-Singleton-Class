# Python-Singleton-Class

This repository has four different ways to implement a singleton class in Python.

A singleton class is one that can only be instantiated once. Subsequent attempts to instantiate it will only create links to the single existing object without changing its state.


=====================================================================================


Este repositorio tiene cuatro formas distintas de implementar una clase singleton en python

Una clase singleton es aquella que sólo puede ser instanciada una única vez, intentos sucesivos de ser instanciada sólo crearion enlaces al único objeto existente sin cambiar el estado que tuviera
